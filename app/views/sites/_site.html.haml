%div[site]
  .container
    %h2= site.name
    %table
      = render :partial => 'watches/watch', :collection => site.watches
    %h3.new_watch= link_to '+ Add URL', new_watch_path(:site_id => site.id)